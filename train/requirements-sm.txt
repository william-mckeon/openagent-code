# Extra deps for the SageMaker PyTorch container (Script Mode).
#
# train/launch_sm.py stages this file into the job's source_dir as `requirements.txt`, which
# SageMaker pip-installs into the training container before running train/sft.py.
#
# The SageMaker PyTorch DLC ALREADY ships torch + numpy matched to its CUDA build, so we DO NOT
# list torch here (pinning it would fight the container's build — the same reason Arcus's
# requirements-sm.txt is torch-free). Only the LoRA-SFT stack train/sft.py imports goes here.
transformers>=4.44.0
peft>=0.12.0
accelerate>=0.33.0
datasets>=2.14.0
bitsandbytes>=0.43.0
safetensors>=0.4.0
sentencepiece>=0.1.99
protobuf>=3.20
