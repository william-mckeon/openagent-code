# =============================================================================
# openagent-code - Git ignore rules
# =============================================================================
# CRITICAL: .env contains secrets. It must NEVER be committed.
# =============================================================================


# --- Secrets ---------------------------------------------------------------
.env
.env.local
.env.*.local
*.pem
*.key
secrets/
# Runtime MCP server config (specs/0029): a copy of mcp.json.example may carry a provider key
# (e.g. TAVILY_API_KEY). The tracked template is mcp.json.example; the real mcp.json is not committed.
mcp.json


# --- Captured training data ------------------------------------------------
# Trajectories can contain source code and prompts from the repos worked on.
# Keep the folder, ignore its contents (including subdirs like trajectories/eval/).
trajectories/*.jsonl
trajectories/**/*.jsonl
workspace/

# Readable per-run session logs (src/logsetup.py) — local operational records, may
# contain code/prompts from the repos worked on. Hand-offable, not committed.
logs/

# Converter output — derived from trajectories, same code/prompt content.
train/dataset/
# Trained student checkpoints / LoRA adapters — large weights, regenerable from the data.
train/checkpoints/
# SageMaker launcher scratch (train/launch_sm.py): staged source + downloaded model artifact.
.sm_src/
.sm_out/

# Cross-session memory (Phase 4 #7) — a per-project notes file the agent writes,
# only present if CODE_MEMORY is on and openagent-code is pointed at this repo.
.openagent/


# --- Python ----------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python


# --- Distribution / packaging ----------------------------------------------
build/
dist/
*.egg-info/
.eggs/
wheels/
*.whl


# --- Virtual environments --------------------------------------------------
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/


# --- Testing / coverage ----------------------------------------------------
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
.hypothesis/


# --- Type checkers ---------------------------------------------------------
.mypy_cache/
.pyright/
.pytype/
.pyre/


# --- Linters / formatters --------------------------------------------------
.ruff_cache/
.black_cache/


# --- IDE / editor ----------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~


# --- OS noise --------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini


# --- Docker (build artifacts only; Dockerfile and compose are tracked) -----
.docker/
*.tar

# adaptive-effort online learner state (Phase 21) - local data, never committed
effort_state.json
*.effort_state.json
